//
// # Enumerated Channel Names
//
// The message-passing style uses two such channels, one for each direction.
//
export const Channels = Object.freeze({
  MainToRenderer: "message-main-to-renderer",
  RendererToMain: "message-renderer-to-main",
});
